export const metadata = {
    title: "User Account",
    description: "User Account",
}

export default function Layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}