export const metadata = {
    title: 'Sign Up',
    description: 'Sign up to Wasl',
}

export default function SignUpLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}